"""Literal types derived from the bundled FtM model.

GENERATED by scripts/regen_model.py — DO NOT EDIT by hand.
Source: model/model.json (commit-pinned FtM model snapshot).
"""
# ruff: noqa
from typing import Literal

Schema = Literal[
{% for name in schema_names %}
    {{ name | py_str }},
{% endfor %}
]

PropertyType = Literal[
{% for name in type_names %}
    {{ name | py_str }},
{% endfor %}
]

Topic = Literal[
{% for name in topic_values %}
    {{ name | py_str }},
{% endfor %}
]

Gender = Literal[
{% for name in gender_values %}
    {{ name | py_str }},
{% endfor %}
]
