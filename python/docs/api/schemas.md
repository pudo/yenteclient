# Schemas

Bundled FtM model snapshot and lookup helpers. Use these when you need
to introspect the model at runtime — `yente-cli ref` is the CLI front-end
for the same data.

::: yente_client.schemas
